jQuery(document).ready(function ($) {

    //==============================================================
    // Match Height
    // ==============================================================
    if($('.equal-box').length > 0) {
        $('.equal-box').matchHeight({byRow: 0});
    }
});